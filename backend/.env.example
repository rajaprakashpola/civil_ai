# Comma-separated list of origins allowed by CORS middleware
BACKEND_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Bind address and port for the development server (uvicorn)
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8010
